// This file contains the model for barcode data
// It contains the BarCodeModel class that has two properties: barcode and date.

class BarCodeModel{
  final String barcode;
  final String date;
  BarCodeModel(this.barcode, this.date);
}